# 프론트엔드 작업 지침

이 파일은 `polar-fe/` 저장소 전체에 적용됩니다. 코드를 수정하기 전에 `docs/CODING_CONVENTIONS.md`를 반드시 읽고 준수합니다.

## 핵심 규칙

- Vue 2와 현재 Vue CLI 5 구성을 유지합니다.
- `.editorconfig`의 2칸 들여쓰기, 줄 끝 공백 제거와 파일 끝 개행 규칙을 따릅니다.
- Vue 컴포넌트는 `PascalCase.vue`, JavaScript 식별자는 `camelCase`를 사용합니다.
- API 호출은 `src/api`, 라우팅은 `src/router`, 공통 상태는 `src/store`의 기존 구조를 따릅니다.
- 비밀값, 서비스 키, 계정 정보와 환경별 URL을 코드에 새로 하드코딩하지 않습니다.
- 예외와 Promise rejection을 무시하지 않습니다.
- 기존 주석과 TODO/FIXME를 삭제하지 않습니다.
- 요청 범위 밖의 리팩터링이나 일괄 포맷 변경을 하지 않습니다.

## 검증

- 린트 검증은 자동 수정을 방지하기 위해 `npm run lint -- --no-fix`를 사용합니다.
- 빌드 영향이 있는 변경은 `npm run build`로 확인합니다.
- 현재 저장소에 기존 린트·빌드 오류가 있다면 수정한 파일에서 새로 발생한 오류와 구분해 보고합니다.
- 검증을 실행하지 못했거나 실패했다면 원인과 미검증 범위를 명확히 보고합니다.
